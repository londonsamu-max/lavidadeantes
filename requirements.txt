# La Vida de Antes — dependencias del pipeline
# Instalar en un entorno virtual propio:  python3 -m venv .venv && source .venv/bin/activate
# Razón: opencv arrastra numpy 2.x y eso rompió tensorflow 2.16 en otro proyecto de esta máquina.

requests==2.32.5                  # descargas y generador de imágenes
edge-tts==7.2.8                   # voz. OJO: la versión importa — de aquí salen los
                                  # SentenceBoundary con los que se sincronizan los subtítulos
pillow==11.3.0                    # miniaturas y hojas de contactos
opencv-contrib-python==5.0.0.93   # dnn_superres (ESPCN x2). El paquete "contrib" es
                                  # obligatorio: el opencv normal NO trae dnn_superres
imageio-ffmpeg==0.6.0             # trae su propio ffmpeg, no hay que instalarlo aparte
mutagen==1.47.0                   # duración real del mp3

google-api-python-client==2.198.0 # subida a YouTube + Analytics
google-auth-oauthlib==1.3.1
google-auth==2.50.0
